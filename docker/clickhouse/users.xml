<?xml version="1.0"?>
<clickhouse>
    <users>
        <!-- Default user without password for development -->
        <default>
            <!-- No password element means no password required -->
            <networks>
                <ip>::/0</ip>
            </networks>
            <profile>default</profile>
            <quota>default</quota>
            <access_management>1</access_management>
        </default>
    </users>
</clickhouse>